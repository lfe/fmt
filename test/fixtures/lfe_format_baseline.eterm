%% Behavior-preservation baseline (slice9): rendered bytes of every
%% pe_lfe_samples form x width, captured from the PRE-slice9 hardcoded
%% call_form dispatch (commit before the registry refactor). The
%% registry-driven path must reproduce these byte-for-byte (A1S9-8).
%% Regenerate ONLY from a known-good pre-refactor tree.
[{lfe_01_ackermann,40,
                   <<"(defun ackermann\n  ((0 n) (+ n 1))\n  ((m 0) (ackermann (- m 1) 1))\n  ((m n)\n    (ackermann (- m 1)\n               (ackermann m (- n 1)))))">>},
 {lfe_01_ackermann,60,
                   <<"(defun ackermann\n  ((0 n) (+ n 1))\n  ((m 0) (ackermann (- m 1) 1))\n  ((m n) (ackermann (- m 1) (ackermann m (- n 1)))))">>},
 {lfe_01_ackermann,80,
                   <<"(defun ackermann\n  ((0 n) (+ n 1))\n  ((m 0) (ackermann (- m 1) 1))\n  ((m n) (ackermann (- m 1) (ackermann m (- n 1)))))">>},
 {lfe_01_ackermann,100,
                   <<"(defun ackermann\n  ((0 n) (+ n 1))\n  ((m 0) (ackermann (- m 1) 1))\n  ((m n) (ackermann (- m 1) (ackermann m (- n 1)))))">>},
 {lfe_02_fizz,40,
              <<"(defun fizz\n  ((0 0 _) \"fizzbuzz\")\n  ((0 _ _) \"fizz\")\n  ((_ 0 _) \"buzz\")\n  ((_ _ n) n))">>},
 {lfe_02_fizz,60,
              <<"(defun fizz\n  ((0 0 _) \"fizzbuzz\")\n  ((0 _ _) \"fizz\")\n  ((_ 0 _) \"buzz\")\n  ((_ _ n) n))">>},
 {lfe_02_fizz,80,
              <<"(defun fizz ((0 0 _) \"fizzbuzz\") ((0 _ _) \"fizz\") ((_ 0 _) \"buzz\") ((_ _ n) n))">>},
 {lfe_02_fizz,100,
              <<"(defun fizz ((0 0 _) \"fizzbuzz\") ((0 _ _) \"fizz\") ((_ 0 _) \"buzz\") ((_ _ n) n))">>},
 {lfe_03_buzz1,40,
               <<"(defun buzz1\n  ((n) (when (== 0 (rem n 5))) \"buzz\")\n  ((_) \"\"))">>},
 {lfe_03_buzz1,60,
               <<"(defun buzz1 ((n) (when (== 0 (rem n 5))) \"buzz\") ((_) \"\"))">>},
 {lfe_03_buzz1,80,
               <<"(defun buzz1 ((n) (when (== 0 (rem n 5))) \"buzz\") ((_) \"\"))">>},
 {lfe_03_buzz1,100,
               <<"(defun buzz1 ((n) (when (== 0 (rem n 5))) \"buzz\") ((_) \"\"))">>},
 {lfe_04_tail_buzz,40,
                   <<"(defun tail-buzz\n  ((n acc)\n    (when (=< n 0))\n    (lists:reverse acc))\n  ((n acc)\n    (tail-buzz (- n 1)\n               (cons (buzz1 n) acc))))">>},
 {lfe_04_tail_buzz,60,
                   <<"(defun tail-buzz\n  ((n acc) (when (=< n 0)) (lists:reverse acc))\n  ((n acc) (tail-buzz (- n 1) (cons (buzz1 n) acc))))">>},
 {lfe_04_tail_buzz,80,
                   <<"(defun tail-buzz\n  ((n acc) (when (=< n 0)) (lists:reverse acc))\n  ((n acc) (tail-buzz (- n 1) (cons (buzz1 n) acc))))">>},
 {lfe_04_tail_buzz,100,
                   <<"(defun tail-buzz\n  ((n acc) (when (=< n 0)) (lists:reverse acc))\n  ((n acc) (tail-buzz (- n 1) (cons (buzz1 n) acc))))">>},
 {lfe_05_plusplus,40,
                  <<"(defmacro ++\n  (() '())\n  ((l) l)\n  ((l . ls)\n    `(lists:append ,l (++ . ,ls))))">>},
 {lfe_05_plusplus,60,
                  <<"(defmacro ++\n  (() '())\n  ((l) l)\n  ((l . ls) `(lists:append ,l (++ . ,ls))))">>},
 {lfe_05_plusplus,80,
                  <<"(defmacro ++ (() '()) ((l) l) ((l . ls) `(lists:append ,l (++ . ,ls))))">>},
 {lfe_05_plusplus,100,
                  <<"(defmacro ++ (() '()) ((l) l) ((l . ls) `(lists:append ,l (++ . ,ls))))">>},
 {lfe_06_cond,40,
              <<"(defmacro cond\n  ((c) c)\n  ((('else . body) . _)\n    `(progn . ,body))\n  (((test . body) . clauses)\n    `(if ,test\n         (progn . ,body)\n         (cond . ,clauses))))">>},
 {lfe_06_cond,60,
              <<"(defmacro cond\n  ((c) c)\n  ((('else . body) . _) `(progn . ,body))\n  (((test . body) . clauses)\n    `(if ,test (progn . ,body) (cond . ,clauses))))">>},
 {lfe_06_cond,80,
              <<"(defmacro cond\n  ((c) c)\n  ((('else . body) . _) `(progn . ,body))\n  (((test . body) . clauses) `(if ,test (progn . ,body) (cond . ,clauses))))">>},
 {lfe_06_cond,100,
              <<"(defmacro cond\n  ((c) c)\n  ((('else . body) . _) `(progn . ,body))\n  (((test . body) . clauses) `(if ,test (progn . ,body) (cond . ,clauses))))">>},
 {lfe_07_bq_expand,40,
                   <<"(eval-when-compile\n  (defun bq-expand\n    ((exp n)\n      (case exp\n        ((tuple 'unquote e)\n          (when (> n 0))\n          (tuple 'unquote\n                 (bq-expand e (- n 1))))\n        ((tuple 'unquote e) e)\n        ((cons 'backquote x)\n          (bq-expand-list exp (+ n 1)))\n        (x (bq-expand-list x n))))))">>},
 {lfe_07_bq_expand,60,
                   <<"(eval-when-compile\n  (defun bq-expand\n    ((exp n)\n      (case exp\n        ((tuple 'unquote e)\n          (when (> n 0))\n          (tuple 'unquote (bq-expand e (- n 1))))\n        ((tuple 'unquote e) e)\n        ((cons 'backquote x) (bq-expand-list exp (+ n 1)))\n        (x (bq-expand-list x n))))))">>},
 {lfe_07_bq_expand,80,
                   <<"(eval-when-compile\n  (defun bq-expand\n    ((exp n)\n      (case exp\n        ((tuple 'unquote e)\n          (when (> n 0))\n          (tuple 'unquote (bq-expand e (- n 1))))\n        ((tuple 'unquote e) e)\n        ((cons 'backquote x) (bq-expand-list exp (+ n 1)))\n        (x (bq-expand-list x n))))))">>},
 {lfe_07_bq_expand,100,
                   <<"(eval-when-compile\n  (defun bq-expand\n    ((exp n)\n      (case exp\n        ((tuple 'unquote e) (when (> n 0)) (tuple 'unquote (bq-expand e (- n 1))))\n        ((tuple 'unquote e) e)\n        ((cons 'backquote x) (bq-expand-list exp (+ n 1)))\n        (x (bq-expand-list x n))))))">>},
 {lfe_08_ets_new,40,
                 <<"(defun new ()\n  (let ((tab\n         (ets:new 'places\n                  (list 'named_table\n                        'public))))\n    (lists:foreach\n      (match-lambda\n        ((`#(,name ,desc))\n          (ets:insert tab\n                      (make-place name\n                                  name\n                                  desc\n                                  desc))))\n      (default-places))\n    tab))">>},
 {lfe_08_ets_new,60,
                 <<"(defun new ()\n  (let ((tab (ets:new 'places (list 'named_table 'public))))\n    (lists:foreach\n      (match-lambda\n        ((`#(,name ,desc))\n          (ets:insert tab\n                      (make-place name name desc desc))))\n      (default-places))\n    tab))">>},
 {lfe_08_ets_new,80,
                 <<"(defun new ()\n  (let ((tab (ets:new 'places (list 'named_table 'public))))\n    (lists:foreach\n      (match-lambda\n        ((`#(,name ,desc)) (ets:insert tab (make-place name name desc desc))))\n      (default-places))\n    tab))">>},
 {lfe_08_ets_new,100,
                 <<"(defun new ()\n  (let ((tab (ets:new 'places (list 'named_table 'public))))\n    (lists:foreach\n      (match-lambda ((`#(,name ,desc)) (ets:insert tab (make-place name name desc desc))))\n      (default-places))\n    tab))">>},
 {lfe_09_by_place_ms,40,
                     <<"(defun by_place_ms (place min)\n  `(#((match-place '_ ,place '$1)\n      ((>= '$1 ,min))\n      ('$1))))">>},
 {lfe_09_by_place_ms,60,
                     <<"(defun by_place_ms (place min)\n  `(#((match-place '_ ,place '$1) ((>= '$1 ,min)) ('$1))))">>},
 {lfe_09_by_place_ms,80,
                     <<"(defun by_place_ms (place min)\n  `(#((match-place '_ ,place '$1) ((>= '$1 ,min)) ('$1))))">>},
 {lfe_09_by_place_ms,100,
                     <<"(defun by_place_ms (place min) `(#((match-place '_ ,place '$1) ((>= '$1 ,min)) ('$1))))">>},
 {lfe_10_mnesia_new,40,
                    <<"(defun new ()\n  (mnesia:create_table 'place\n                       `(#(attributes\n                           ,(fields 'place))\n                         #(disc_copies\n                           ((node)))\n                         #(type 'set))))">>},
 {lfe_10_mnesia_new,60,
                    <<"(defun new ()\n  (mnesia:create_table 'place\n                       `(#(attributes ,(fields 'place))\n                         #(disc_copies ((node)))\n                         #(type 'set))))">>},
 {lfe_10_mnesia_new,80,
                    <<"(defun new ()\n  (mnesia:create_table 'place\n                       `(#(attributes ,(fields 'place))\n                         #(disc_copies ((node)))\n                         #(type 'set))))">>},
 {lfe_10_mnesia_new,100,
                    <<"(defun new ()\n  (mnesia:create_table 'place\n                       `(#(attributes ,(fields 'place)) #(disc_copies ((node))) #(type 'set))))">>},
 {lfe_11_guess_server,40,
                      <<"(defun guess-server (state)\n  (receive\n    (#('guess from n)\n      (when (is_integer n))\n      (! from (check state n))\n      (guess-server state))\n    ('stop 'ok)))">>},
 {lfe_11_guess_server,60,
                      <<"(defun guess-server (state)\n  (receive\n    (#('guess from n)\n      (when (is_integer n))\n      (! from (check state n))\n      (guess-server state))\n    ('stop 'ok)))">>},
 {lfe_11_guess_server,80,
                      <<"(defun guess-server (state)\n  (receive\n    (#('guess from n)\n      (when (is_integer n))\n      (! from (check state n))\n      (guess-server state))\n    ('stop 'ok)))">>},
 {lfe_11_guess_server,100,
                      <<"(defun guess-server (state)\n  (receive\n    (#('guess from n) (when (is_integer n)) (! from (check state n)) (guess-server state))\n    ('stop 'ok)))">>},
 {lfe_12_ping_pong,40,
                   <<"(progn\n  (defun handle_call\n    (('ping _from state)\n      #('reply\n        'pong\n        (set-state-pings state\n                         (+ (state-pings state)\n                            1)))))\n  (defun handle_cast\n    (('pong state) #('noreply state))))">>},
 {lfe_12_ping_pong,60,
                   <<"(progn\n  (defun handle_call\n    (('ping _from state)\n      #('reply\n        'pong\n        (set-state-pings state (+ (state-pings state) 1)))))\n  (defun handle_cast (('pong state) #('noreply state))))">>},
 {lfe_12_ping_pong,80,
                   <<"(progn\n  (defun handle_call\n    (('ping _from state)\n      #('reply 'pong (set-state-pings state (+ (state-pings state) 1)))))\n  (defun handle_cast (('pong state) #('noreply state))))">>},
 {lfe_12_ping_pong,100,
                   <<"(progn\n  (defun handle_call\n    (('ping _from state) #('reply 'pong (set-state-pings state (+ (state-pings state) 1)))))\n  (defun handle_cast (('pong state) #('noreply state))))">>},
 {lfe_13_get_page,40,
                  <<"(defun get-page (url)\n  (let ((#('ok id)\n         (httpc:request 'get\n                        #(url ())\n                        ()\n                        (#(sync\n                           'false)))))\n    (receive\n      (#('http #(id 'result body))\n        #('ok body))\n      (#('http #(id 'error reason))\n        #('error reason)))))">>},
 {lfe_13_get_page,60,
                  <<"(defun get-page (url)\n  (let ((#('ok id)\n         (httpc:request 'get\n                        #(url ())\n                        ()\n                        (#(sync 'false)))))\n    (receive\n      (#('http #(id 'result body)) #('ok body))\n      (#('http #(id 'error reason)) #('error reason)))))">>},
 {lfe_13_get_page,80,
                  <<"(defun get-page (url)\n  (let ((#('ok id) (httpc:request 'get #(url ()) () (#(sync 'false)))))\n    (receive\n      (#('http #(id 'result body)) #('ok body))\n      (#('http #(id 'error reason)) #('error reason)))))">>},
 {lfe_13_get_page,100,
                  <<"(defun get-page (url)\n  (let ((#('ok id) (httpc:request 'get #(url ()) () (#(sync 'false)))))\n    (receive\n      (#('http #(id 'result body)) #('ok body))\n      (#('http #(id 'error reason)) #('error reason)))))">>},
 {lfe_14_fish_closure,40,
                      <<"(defun fish-class (species\n                   weight\n                   children)\n  (lambda (method args)\n    (case method\n      ('species species)\n      ('weight weight)\n      ('grow\n        (fish-class species\n                    (+ weight\n                       (car args))\n                    children)))))">>},
 {lfe_14_fish_closure,60,
                      <<"(defun fish-class (species weight children)\n  (lambda (method args)\n    (case method\n      ('species species)\n      ('weight weight)\n      ('grow\n        (fish-class species\n                    (+ weight (car args))\n                    children)))))">>},
 {lfe_14_fish_closure,80,
                      <<"(defun fish-class (species weight children)\n  (lambda (method args)\n    (case method\n      ('species species)\n      ('weight weight)\n      ('grow (fish-class species (+ weight (car args)) children)))))">>},
 {lfe_14_fish_closure,100,
                      <<"(defun fish-class (species weight children)\n  (lambda (method args)\n    (case method\n      ('species species)\n      ('weight weight)\n      ('grow (fish-class species (+ weight (car args)) children)))))">>},
 {lfe_15_fish_process,40,
                      <<"(defun fish-class (species\n                   weight\n                   children)\n  (receive\n    (#('weight from)\n      (! from weight)\n      (fish-class species\n                  weight\n                  children))\n    (#('feed amount)\n      (fish-class species\n                  (+ weight amount)\n                  children))))">>},
 {lfe_15_fish_process,60,
                      <<"(defun fish-class (species weight children)\n  (receive\n    (#('weight from)\n      (! from weight)\n      (fish-class species weight children))\n    (#('feed amount)\n      (fish-class species (+ weight amount) children))))">>},
 {lfe_15_fish_process,80,
                      <<"(defun fish-class (species weight children)\n  (receive\n    (#('weight from) (! from weight) (fish-class species weight children))\n    (#('feed amount) (fish-class species (+ weight amount) children))))">>},
 {lfe_15_fish_process,100,
                      <<"(defun fish-class (species weight children)\n  (receive\n    (#('weight from) (! from weight) (fish-class species weight children))\n    (#('feed amount) (fish-class species (+ weight amount) children))))">>},
 {lfe_16_account,40,
                 <<"(defun account-class (balance\n                      history\n                      owner)\n  (receive\n    (#('deposit amount from)\n      (cond\n        ((> amount 0)\n          (account-class (+ balance\n                            amount)\n                         (cons amount\n                               history)\n                         owner))\n        ('true\n          (! from 'invalid)\n          (account-class balance\n                         history\n                         owner))))))">>},
 {lfe_16_account,60,
                 <<"(defun account-class (balance history owner)\n  (receive\n    (#('deposit amount from)\n      (cond\n        ((> amount 0)\n          (account-class (+ balance amount)\n                         (cons amount history)\n                         owner))\n        ('true\n          (! from 'invalid)\n          (account-class balance history owner))))))">>},
 {lfe_16_account,80,
                 <<"(defun account-class (balance history owner)\n  (receive\n    (#('deposit amount from)\n      (cond\n        ((> amount 0)\n          (account-class (+ balance amount) (cons amount history) owner))\n        ('true (! from 'invalid) (account-class balance history owner))))))">>},
 {lfe_16_account,100,
                 <<"(defun account-class (balance history owner)\n  (receive\n    (#('deposit amount from)\n      (cond\n        ((> amount 0) (account-class (+ balance amount) (cons amount history) owner))\n        ('true (! from 'invalid) (account-class balance history owner))))))">>},
 {lfe_17_eval_expr,40,
                   <<"(defun eval-expr (e env)\n  (case e\n    (#('quote x) x)\n    (#('cons h t)\n      (cons (eval-expr h env)\n            (eval-expr t env)))\n    (#('if test then else)\n      (eval-if test then else env))\n    (#('lambda args body)\n      (make-lambda args body env))\n    (x (eval-application x env))))">>},
 {lfe_17_eval_expr,60,
                   <<"(defun eval-expr (e env)\n  (case e\n    (#('quote x) x)\n    (#('cons h t)\n      (cons (eval-expr h env) (eval-expr t env)))\n    (#('if test then else) (eval-if test then else env))\n    (#('lambda args body) (make-lambda args body env))\n    (x (eval-application x env))))">>},
 {lfe_17_eval_expr,80,
                   <<"(defun eval-expr (e env)\n  (case e\n    (#('quote x) x)\n    (#('cons h t) (cons (eval-expr h env) (eval-expr t env)))\n    (#('if test then else) (eval-if test then else env))\n    (#('lambda args body) (make-lambda args body env))\n    (x (eval-application x env))))">>},
 {lfe_17_eval_expr,100,
                   <<"(defun eval-expr (e env)\n  (case e\n    (#('quote x) x)\n    (#('cons h t) (cons (eval-expr h env) (eval-expr t env)))\n    (#('if test then else) (eval-if test then else env))\n    (#('lambda args body) (make-lambda args body env))\n    (x (eval-application x env))))">>},
 {lfe_18_parse_bitspecs,40,
                        <<"(defun parse-bitspecs (specs val env)\n  (let ((#(size type)\n         (parse-type specs env)))\n    (case type\n      ('integer\n        (binary (val\n                 (size size)\n                 (unit 1))))\n      ('binary\n        (binary (val\n                 (size size)\n                 'binary))))))">>},
 {lfe_18_parse_bitspecs,60,
                        <<"(defun parse-bitspecs (specs val env)\n  (let ((#(size type) (parse-type specs env)))\n    (case type\n      ('integer (binary (val (size size) (unit 1))))\n      ('binary (binary (val (size size) 'binary))))))">>},
 {lfe_18_parse_bitspecs,80,
                        <<"(defun parse-bitspecs (specs val env)\n  (let ((#(size type) (parse-type specs env)))\n    (case type\n      ('integer (binary (val (size size) (unit 1))))\n      ('binary (binary (val (size size) 'binary))))))">>},
 {lfe_18_parse_bitspecs,100,
                        <<"(defun parse-bitspecs (specs val env)\n  (let ((#(size type) (parse-type specs env)))\n    (case type\n      ('integer (binary (val (size size) (unit 1))))\n      ('binary (binary (val (size size) 'binary))))))">>},
 {lfe_19_eval_lambda,40,
                     <<"(defun eval-lambda\n  ((() _env) '())\n  (((a1) env) (list (eval-expr a1 env)))\n  (((a1 a2) env)\n    (list (eval-expr a1 env)\n          (eval-expr a2 env)))\n  (((cons a as) env)\n    (cons (eval-expr a env)\n          (eval-lambda as env))))">>},
 {lfe_19_eval_lambda,60,
                     <<"(defun eval-lambda\n  ((() _env) '())\n  (((a1) env) (list (eval-expr a1 env)))\n  (((a1 a2) env)\n    (list (eval-expr a1 env) (eval-expr a2 env)))\n  (((cons a as) env)\n    (cons (eval-expr a env) (eval-lambda as env))))">>},
 {lfe_19_eval_lambda,80,
                     <<"(defun eval-lambda\n  ((() _env) '())\n  (((a1) env) (list (eval-expr a1 env)))\n  (((a1 a2) env) (list (eval-expr a1 env) (eval-expr a2 env)))\n  (((cons a as) env) (cons (eval-expr a env) (eval-lambda as env))))">>},
 {lfe_19_eval_lambda,100,
                     <<"(defun eval-lambda\n  ((() _env) '())\n  (((a1) env) (list (eval-expr a1 env)))\n  (((a1 a2) env) (list (eval-expr a1 env) (eval-expr a2 env)))\n  (((cons a as) env) (cons (eval-expr a env) (eval-lambda as env))))">>},
 {lfe_20_eval_receive,40,
                      <<"(defun eval-receive (clauses env)\n  (fletrec\n    ((loop (q)\n       (receive\n         (msg\n           (when (match-clauses msg\n                                clauses))\n           (apply-clause msg\n                         clauses\n                         env))\n         (after timeout\n           (loop (merge-queue q))))))\n    (loop ())))">>},
 {lfe_20_eval_receive,60,
                      <<"(defun eval-receive (clauses env)\n  (fletrec\n    ((loop (q)\n       (receive\n         (msg\n           (when (match-clauses msg clauses))\n           (apply-clause msg clauses env))\n         (after timeout (loop (merge-queue q))))))\n    (loop ())))">>},
 {lfe_20_eval_receive,80,
                      <<"(defun eval-receive (clauses env)\n  (fletrec\n    ((loop (q)\n       (receive\n         (msg (when (match-clauses msg clauses)) (apply-clause msg clauses env))\n         (after timeout (loop (merge-queue q))))))\n    (loop ())))">>},
 {lfe_20_eval_receive,100,
                      <<"(defun eval-receive (clauses env)\n  (fletrec\n    ((loop (q)\n       (receive\n         (msg (when (match-clauses msg clauses)) (apply-clause msg clauses env))\n         (after timeout (loop (merge-queue q))))))\n    (loop ())))">>}].
